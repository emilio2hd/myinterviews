<% unless current_page.first? %>
<li><%= link_to raw(t 'views.pagination.first'), url, remote: remote %></li>
<% else %>
<li class="disabled">
  <%= link_to raw(t 'views.pagination.first'), 'javascript:void(0);' %>
</li>
<% end %>