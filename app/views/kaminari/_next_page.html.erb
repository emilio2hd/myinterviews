<% unless current_page.last? %>
  <li><%= link_to raw(t 'views.pagination.next'), url, rel: 'next', remote: remote %></li>
<% else %>
  <li class="disabled">
    <%= link_to raw(t 'views.pagination.next'), 'javascript:void(0);' %>
  </li>
<% end %>