<% content_header t('my_applications.head_title'), 'Editing' %>

<div class="row">
  <div class="col-lg-12">
    <div class="box box-info">
      <div class="box-header with-border">
        <h3 class="box-title"><%= t('my_applications.box_title') %></h3>
      </div><!-- /.box-header -->

      <!-- form start -->
      <%= form_for(@my_application, html: { class: 'form-horizontal' }) do |form| %>
          <%= render 'form', form: form %>
          <div class="box-footer">
            <%= form.submit t('button.save'), class: 'btn btn-primary', data: { testid: 'myApplicationSaveButton' } %>
            <%= link_to t('button.cancel'), @my_application, class: 'btn btn-default' %>
          </div>
          <!-- /.box-footer -->
      <% end %>
      <!-- /.form -->
    </div>
  </div>
</div>
