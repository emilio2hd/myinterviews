<div class="alert alert-<%= type %> alert-dismissable" data-testid="alertMessage">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
  <% if message.kind_of?(Array) %>
      <% message.each do |item| %>
          <p><%= item %></p>
      <% end %>
  <% else %>
      <%= message %>
  <% end %>
</div>